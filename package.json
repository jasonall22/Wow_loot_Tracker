{
  "name": "apoc-raid-portal",
  "version": "0.2.0-foundation.1",
  "private": true,
  "type": "module",
  "description": "Separate online permission and data-access foundation for APOC; not the installed tracker.",
  "engines": { "node": "24.x" },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs"
  }
}
